

import { Spinner } from '@/components/Elements';
import { useState } from 'react';
import { MoviesInfoTypes, MovieTypes } from '../types';


type MovieInfoProps = {

};

export const MovieInfo = ({  }: MovieInfoProps) => {
  return (
    <div>
      <h1>Info</h1>
    </div>
  );
};
